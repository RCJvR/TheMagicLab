{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "magic-lab",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import http.server, os; http.server.test(HandlerClass=http.server.SimpleHTTPRequestHandler, port=int(os.environ.get('PORT', 3000)))"],
      "port": 3000,
      "autoPort": true
    }
  ]
}
